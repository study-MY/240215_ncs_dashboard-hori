import { mode } from "@chakra-ui/theme-tools";

export const cardStyles = {
  components: {
    Card: {
      baseStyle: {
        container: { borderRadius: "20px", boxShadow: "none" },
      },
    },
  },
};
